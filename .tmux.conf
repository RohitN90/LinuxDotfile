unbind r
bind r source-file ~/.tmux.conf
unbind C-b

set -g prefix `
bind ` send-prefix

set -g base-index 1
set -g pane-base-index 1

# select panel
bind-key h select-pane -L
bind-key j select-pane -D
bind-key k select-pane -U
bind-key l select-pane -R


# Look and feel

set -g default-terminal "${TERM}"
set -sg terminal-overrides ",*:RGB"

set -g status-style bg=default
set -g status-left ""
set -g status-right "%a %d %b %H:%M #[fg=yellow,bright]#S "
set -g renumber-windows on
set -g display-panes-active-colour magenta

# status bar
set -g window-status-format " #W "
set -g window-status-current-format " #W "
set -g window-status-bell-style "bg=red,nobold"

set-window-option -g window-status-current-style bg=magenta,fg=colour237

#set -g window-status-current-style \
    "#{?window_zoomedflag,bg=yellow,bg=magenta,nobold}"

# panel

set -g pane-border-lines simple
set -g pane-border-style fg=black,bright
set -g pane-active-border-style bg=magenta

#Qol

set -g escape-time 0
set -g mouse on
set -g history-limit 1000

# keybines
bind - split-window -v
bind | split-window -h

bind f resize-pane -Z
